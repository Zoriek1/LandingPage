# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.env
.env.*
!.env.example
.google-business-token.json

node_modules
dist
dist-ssr
output
*.local

# Lighthouse before/after runs (LCP measurement pass, see AGENTS.md/PR notes)
perf-artifacts/

# Playwright e2e screenshot evidence (local only, never committed)
tests/e2e/__screenshots__/

# Playwright test output (local only)
test-results/

# Agent workspace (local only, never committed)
.omo/

# Claude Code local settings
.claude/settings.local.json
.claude/memory/

# Isolated agent worktrees
.worktrees/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
